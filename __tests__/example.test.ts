/**
 * Example test file
 * Add your tests here as the project grows
 */

describe('Example Test Suite', () => {
  it('should pass a basic test', () => {
    expect(1 + 1).toBe(2);
  });

  it('should handle string operations', () => {
    const str = 'Linkly';
    expect(str.toLowerCase()).toBe('linkly');
  });
});

